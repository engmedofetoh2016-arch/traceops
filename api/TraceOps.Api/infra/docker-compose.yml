version: "3.9"
services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: traceops
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 1992
    ports:
      -  "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
